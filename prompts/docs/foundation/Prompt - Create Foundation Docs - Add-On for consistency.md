Additional consistency constraints:

- Use the same canonical term names across all foundation docs.
- If a vocabulary value appears in one file, use the same wording in the others.
- Keep the distinction between:
  - source hierarchy
  - metadata schema
  - controlled vocabulary
  - chunking rules
  - benchmark structure
  - system prompt behavior
  clear and non-overlapping.
- Preserve the distinction between:
  - product-specific
  - case-specific
  - historically documented
  - well established
  - mixed evidence
  - uncertain
  where those ideas are relevant.
- Make the foundation docs cleanly extensible into later JSON schemas and policy files.
