Additional consistency rules:

- Treat terminology as canonical across all documents.
- If one file defines an enum or vocabulary, the others must use the same values.
- Policies must be operationally stronger than foundation docs.
- Do not allow the same concept to have different names in different files.
- If a rule depends on provenance, source hierarchy, or review state, make that explicit.
- The documentation must be suitable for direct translation into schemas and code later.
